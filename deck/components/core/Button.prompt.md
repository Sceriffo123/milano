Pulsante istituzionale URITAXI · FILT CGIL — azioni d'avvio e CTA, angoli minimi, peso 700, nessun gradiente.

```jsx
<Button variant="primary" size="lg" onClick={start}>▶ Modalità Relatore</Button>
<Button variant="ghost">⟳ Modalità Vetrina</Button>
```

Varianti: `primary` (rosso pieno, default), `dark` (nero su chiaro), `ghost` (contorno, eredita il colore del testo — ideale su scene scure). Dimensioni `sm | md | lg`. Supporta `disabled`. Le glifo-icone (▶ ⟳ ↓) si scrivono nel testo come carattere Unicode.
